<!--
  Copy this file to posts/<your-slug>.md, then add a matching entry
  to posts/manifest.js. This file itself is never listed — nothing
  reads posts/_template.md unless you rename it.

  Everything below the comment is plain Markdown: headings, paragraphs,
  lists, `code`, code blocks, > blockquotes, and tables all work.
  If your post has an interactive widget, mount it wherever you like
  in the text with a single line:

      {{widget}}

  and it will be dropped in as a docked panel at that point in the
  article. Leave it out entirely for a text-only post.
-->

## The idea

One or two paragraphs introducing the maths concept in plain language.
What's the question you're actually answering?

{{widget}}

## What it shows

Walk through the result. What did the simulation/visualisation reveal?
Include a concrete number or observation, not just "it worked."

## Under the hood

Briefly explain the algorithm or the maths. A short code snippet is
fine here:

```python
# example
```
